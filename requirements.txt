# Versione moderna e stabile di pytonlib
pytonlib>=0.0.70

# Tonsdk dalla release ufficiale su PyPI[citation:1]
tonsdk

# Dipendenze TON (potrebbero non essere necessarie, prova a toglierle se causano conflitto)
ton==0.26
tvm_valuetypes==0.0.9

# Dipendenze principali
aiohttp==3.8.4
requests==2.28.2
telepot==12.7

# Dipendenze di sistema (compatibili con le versioni sopra)
attrs==23.1.0
bitarray==2.6.0
certifi>=2023.7.22
cffi==1.15.1
crc16==0.1.1
crc32c==2.3.post0
PyNaCl==1.5.0
urllib3==1.26.15
